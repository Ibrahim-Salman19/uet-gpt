# Staleness MVP — Closed Freshness Lifecycle

**Branch:** `agent/2026-07-28-staleness-mvp` from `e7136c0` (HEAD, carries recovery-hold `518076e`); cherry-pick `c1eb2e6` to recover `convex/rag/smokeRetrieval.ts`.

**Locked decisions:** (1) Use existing schema status literals only — eligible=`{active,indexed}`, hard-excluded=`{stale,failed,pending,processing,pending_embed}`; superseded/quarantined/deleted/withdrawn deferred (no migration). (2) Cherry-pick `c1eb2e6`'s `smokeRetrieval.ts` and extend it. (3) Cron: 03:30 flag (bounded sweep + `runAfter`), 04:00 observe; 03:00 contextualize untouched.

**Reconciled facts:** `isStale` written only at `staleness.ts:131` (never cleared); `lastVerifiedAt` absent (deferred); `metadata.etag`/`lastModified` stored but never read; `search.ts:255` `.slice(0,limit)` before enrichment discards overfetch (must remove); rerank (`retrieval.ts:209-241`) overwrites `relevanceScore` after search → overfetch+exclusion must be internal to `searchDocumentsAction`; one caller `retrieval.ts:197`; no "30 known failures" baseline exists → establish fresh at Gate 1.

## NEW files (4)

**1. `convex/shared/freshnessPolicy.ts`** (pure module, no Convex runtime import): `FRESHNESS_POLICY_VERSION="2026-07-staleness-mvp-1"`, `FRESHNESS_POLICY_OWNER`, `FRESHNESS_POLICY_LAST_REVIEWED`, `FRESHNESS_TTL_DAYS={high:14,medium:60,low:180}`, `FRESHNESS_TTL_MS`, `DEFAULT_STALE_SCORE_MULTIPLIER=0.3`, `STALE_MULTIPLIER_GRID=[0,0.1,0.3,0.5,1.0]`, `STALE_OVERFETCH_FACTOR=3`, `MIN_CANDIDATES=30`, `MAX_CANDIDATES=256`, `candidateLimit(k)=min(256,max(k*3,30))`, `RETRIEVAL_ELIGIBLE_STATUSES=["active","indexed"]`, `RETRIEVAL_EXCLUDED_STATUSES=["stale","failed","pending","processing","pending_embed"]`, `HIGH_IMPACT_KEYWORDS=[fee,deadline,merit,admission,date sheet,eligibility,...]`, `MEDIUM_IMPACT_KEYWORDS=[contact,faculty,department,program]`. Pure helpers (accept `now` arg, no `Date.now`): `classifyFreshness({status,isStale,crawledAt,freshnessTier,now})→{state:fresh|aged|unknown,applicability:current|unknown,eligible,penalized}`, `classifyQueryRisk(query)→high|medium|low` (keyword, deterministic NOT LLM), `shouldAbstainOnStaleOnly({risk,allSourcesAgedOrUnknown,anyFreshSource})→bool` (high-impact + no fresh → abstain; medium/low never force-abstain). Status in excluded set → `{state:unknown, eligible:false}`; missing crawledAt → `{state:unknown, applicability:unknown}` (NEVER fresh); isStale OR older-than-TTL → `{state:aged, penalized:true}`.

**2. `tests/unit/freshness-policy.test.ts`** — pure-function tests: TTL boundaries (13d fresh/15d aged high; 59/61 medium; 179/181 low), missing crawledAt→unknown-never-fresh, future ts→fresh, status precedence (stale→excluded even if recent crawledAt), `classifyQueryRisk` hits/misses, `shouldAbstainOnStaleOnly` truth table, `candidateLimit` (k=8→30, k=100→256 cap, k=5→30 floor).

**3. `tests/unit/staleness-sweep.test.ts`** — mocks `ctx.db.query().paginate()` + `ctx.scheduler.runAfter`: dryRun zero writes; first batch returns `{complete:false,nextCursor}`; cursor continuation flags remainder; idempotency (re-run→`flagged:0,alreadyStale:N`); duplicate-cron (active sweep present→no-op); batch bounded by `BATCH_SIZE=100`.

**4. `docs/runbooks/staleness-mvp.md`** + **`docs/audit/STALENESS_MVP_MANIFEST.json`** (Gate 0).

## MODIFIED files (8) — exact anchors

**5. `convex/crawl/staleness.ts`** — replace `flagExpiredDocuments` body (lines 98-144) with cursor-based resumable sweep. Keep `purgeStaleDocuments`+`markStaleDocuments` UNCHANGED (no auto-purge in MVP). New args: `{limit?, dryRun?, cursor?, sweepId?, startedAt?, cumulative?}`. Returns `{sweepId,examined,flagged,alreadyStale,skipped,nextCursor|null,complete,dryRun}`. Logic: resolve sweep state (new vs continuation); `.paginate({numItems:100,cursor})` (reliable cursor advance, not `.take`); per-doc `classifyFreshness` → aged&&!isStale → (dryRun?count:`patch isStale:true`); accumulate; if `!isDone` → `ctx.scheduler.runAfter(0, internal.crawl.staleness.flagExpiredDocuments, {cursor,sweepId,startedAt,cumulative,dryRun})`. NEVER write `isStale:false` (age flagger never reactivates — amendment #7).

**6. `convex/crons.ts`** — insert before `staleness-check` (line 91): `crons.cron("staleness-flag-expired", {hourUTC:3,minuteUTC:30}, internal.crawl.staleness.flagExpiredDocuments, {})`. Existing 04:00 observe stays. Update comment.

**7. `convex/observability/staleness.ts` + `internal.ts`** — `checkStaleness` output gains `lastSweepStartedAt,lastSweepCompletedAt,sweepComplete,documentsExamined,documentsFlagged,documentsCleared,cursorRemaining,errors`. No completed sweep by 04:00 → explicit `sweepComplete:false` (no silent partial). `computeStaleness` (internal.ts:169-208) imports TTLs from `freshnessPolicy.ts` (eliminates the threshold-mismatch bug — single source).

**8. `convex/embeddings/doc_queries.ts`** — add `isStale?,status?` to: `DocQueryResult` type (6-15), validator inner doc (100-109), chunk-path build (174-188), fallback build (194-206), `getDocumentByEntryId` validator (36-45)+returns (56-69,80-89).

**9. `convex/embeddings/search.ts`** — `DocMeta` (34-42)+=`isStale?,status?`; `batchFetchDocMeta` (96-104) populate them; `searchLimit=20` (213)→`candidateLimit(limit)`; chunk-text `limit:10` (242)→`candidateLimit(limit)`; **REMOVE `.slice(0,limit)` at 255**, replace with `.slice(0,searchLimit)` (let overfetch survive); after enrichment (259-277) before sort (279) insert: filter out `status∈EXCLUDED_STATUSES`, then `map` applying `penalized? score*DEFAULT_STALE_SCORE_MULTIPLIER : score`. Final `.slice(0,limit)` (288) stays. Add `freshnessState?,freshnessTier?` to result validator (177-186)+TS type (190-192).

**10. `convex/rag/context.ts`** — `buildContext` (10-77): chunks arg +=`crawledAt?,freshnessTier?,isStale?,status?`. Per chunk emit (machine-computed via `classifyFreshness`):
```
Source: [title](url)
Retrieved: 2026-07-20
Verified: unavailable
Freshness tier: high
Freshness state: fresh
Applicability: current

{content}
---
```
`Verified:` always `unavailable` (no lastVerifiedAt — honest). Missing crawledAt→"Retrieved: unavailable"+state `unknown`. Section label (38-40) stays.

**11. `convex/rag/prompts.ts`** — add SYSTEM_PROMPT rules after rule 9: (10) treat Freshness state/Applicability metadata authoritative, don't infer aged/unknown is current; (11) HIGH-IMPACT ABSTENTION — fees/deadlines/merit/schedules/eligibility/datesheets/admissions: don't present as current unless ≥1 source is fresh+current; else direct to official site; (12) historical→state exact retrieval date/session; (13) never hide source disagreement; (14) never fabricate retrieval/verification dates.

**12. `convex/rag/retrieval.ts`** — `SearchResult` (176-183)+=`freshnessState?,freshnessTier?,crawledAt?,isStale?,status?`. After `finalResults` determined (post-rerank+CRAG, before `buildResponseContext`): `classifyQueryRisk(question)`→risk; `anyFresh=finalResults.some(r=>r.freshnessState==="fresh")`; `allAgedOrUnknown=finalResults.length>0&&finalResults.every(r=>r.freshnessState!=="fresh")`; if `shouldAbstainOnStaleOnly({risk,allAgedOrUnknown,anyFresh})`→return `{intent:"stale-only-high-impact-abstain",context:"",sources:[],cachedResponse:null,queryEmbedding}`. `buildResponseContext` (373-383) pass `crawledAt,freshnessTier,isStale,status` to `buildContext`.

**13. `convex/crawl/mutations.ts`** — clear `isStale:false` on successful recrawl (amendment #7) at 6 sites: 184, 202, 303-309 (Path A); 812-820, 844, 868 (Path B). Unchanged-fast-path = "successful unchanged verification" → clears isStale, retains content, bumps crawledAt. Age flagger never writes false → no reactivation race.

## EXTENDED (1)

**14. `convex/rag/smokeRetrieval.ts`** (cherry-picked from `c1eb2e6`) — extend `SmokeResult`/`SmokeReturn` with `freshnessState,freshnessTier,applicability,penalized,excludedReason` per amendment #8.

## Gates

**Gate 0:** manifest with branch, base `e7136c0`, cherry-pick `c1eb2e6`, deploy `confident-viper-402`, fresh usage snapshot, cron snapshot, recovery-hold unchanged, no new backup, no env values.
**Gate 1:** `pnpm typecheck`→0 errors. Establish baseline on `e7136c0` BEFORE changes — record every failing test by exact `file::test-name` (no pre-existing "30" baseline exists). After changes: `pnpm vitest run`→zero NEW failures + all new staleness tests pass. `python -m py_compile scripts/*.py`→0.
**Gate 2:** `pnpm convex dev` local — `flagExpiredDocuments({dryRun:true})`, confirm counts+zero writes; overfetch+exclusion doesn't starve top-k. Grep-verify: no unbounded `.collect()`, no schema/vector-index change, no embeddingDimension change, no HMAC change, no sync-embed.
**Gate 3 (parallel):** Gemini key type, last successful contextualize/embed, model-not-found errors since June 1, Groq Aug-16. Runs in parallel with code; crawl pilot blocked until pass, but staleness-MVP deploy does NOT depend on it.
**Gate 4:** Deploy `confident-viper-402`. `flagExpiredDocuments({dryRun:true})` via `convex run` — expected IDs/counts, zero writes, acceptable I/O.
**Gate 5:** One bounded `flagExpiredDocuments({limit:100})`. Inspect counters+DB I/O. `smokeRetrieval` confirms: fresh rank-1, isStale-penalty, status:stale hard-excluded, overfetch recovers fresh-below-stale, high-impact stale-only abstains, recrawl clears TTL staleness, explicit invalidation NOT cleared.
**Gate 6:** Pause `adamant-stork-623` via Convex deployment control (NOT cron edit/delete). Pre-pause checklist in manifest. Verify: harmless call→paused error, crons stop, usage flat, storage intact.
**Gate 7:** 8-10 doc pilot (2 high HTML, 2 medium, 2 low, 1 PDF/table, 1 changed fixture, 1 stale negative). 5 stages: local→prod dry→prod mutation→retrieval lifecycle→answer policy. Per-doc-type cost (HTML vs PDF, 30%/50% reserve).

## Deferred

Schema migration (new status literals, lastVerifiedAt, supersededBy, sourceVersionId), ETag/304 conditional-GET in crawler.py, auto-purge, version chains, academic-session extraction, conflict adjudication, vector-index filterField change, Crawl4AI re-enable, full re-embed, hosted ablations.

## Execution order

1. Gate 0: branch from `e7136c0` + cherry-pick `c1eb2e6` + manifest.
2. Gate 1 baseline (exact failing tests on `e7136c0`).
3. **Parallel:** Gate 3 provider audit (read-only).
4. `convex/shared/freshnessPolicy.ts`.
5. `tests/unit/freshness-policy.test.ts` green.
6. `staleness.ts` cursor sweep + `crons.ts` 03:30 + observability sweep-state.
7. Plumb isStale/status: `doc_queries.ts`→`search.ts` DocMeta.
8. Overfetch+exclusion+penalty in `search.ts` (remove line-255 slice).
9. Stale-clear in `mutations.ts` (6 sites).
10. `context.ts` metadata + `prompts.ts` rules + `retrieval.ts` abstention/plumbing.
11. Extend `smokeRetrieval.ts`.
12. `staleness-sweep.test.ts` + integration. Gate 1 full.
13. Gate 2 local. Commit. Deploy `confident-viper-402`.
14. Gates 4→5.
15. Gate 6 pause adamant-stork-623.
16. Gate 7 pilot + cost model → approve/reject post-reset crawl.

## Constraints (verbatim)

No schema vector-index filterField/embeddingDimension/HMAC/sync-embed changes; no `git push --force`; no `git add -A` (explicit paths); no main commits; pnpm ONLY; every metric MEASURED or labeled UNVERIFIED/OBSERVED/INFERRED/BLOCKED (no fabrication); no env values recorded (names only); bandwidth measured before/after every Convex op, 50MB stop, 25% monthly reserve, no bulk op without pilot; recovery hold unchanged; adamant-stork-623 pause via Convex control (reversible, not deleted); no full crawl until pilot validates cost AND Gate 3 passes. Stop conditions: unexpected DB usage spike, >1 recovery doc processed, embed/persist fails after contextualize succeeds, doc marked complete after partial failure, deployed SHA includes unrelated commits, fallback rotates all keys, targets wrong deployment, V8 fatal, prompts reconfigure, creates/selects new deployment, prints sensitive values.