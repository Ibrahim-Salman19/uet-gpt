# Phase 0A Handoff — Completion Checklist

**Snapshot:** `docs/audit/SNAPSHOT_20260726T094113Z.json`
**Date:** 2026-07-26T09:41:13Z
**Authorization scope:** Phase 0A only (read-only snapshot, no behavioral change).

This document addresses each item in the Phase-0A completion checklist with explicit status. Items marked **NEEDS-YOU** require your action (auth, approval, or information) before Phase 0B.

---

## Completion checklist

| # | Required artifact | Status | Location / note |
|---|---|---|---|
| 1 | Annotated or signed snapshot tag | ✅ DONE | `audit/snapshot-20260726T094113Z` (annotated tag on `agent/2026-07-26-phase-0a-snapshot`) |
| 2 | Cryptographic manifest | ✅ DONE | `docs/audit/SNAPSHOT_20260726T094113Z.json` (full sha256 prefixes for 22 critical files) |
| 3 | Local commit identifier | ✅ DONE | `d5889da145a19c57e7878f88488e6f97f760b2db` (main) |
| 4 | Deployed (production) commit identifier | ⚠️ **NEEDS-YOU** | Not exposed in `uet-gpt.vercel.app` rendered HTML; requires Vercel API auth |
| 5 | Vercel deployment identifier | ⚠️ **NEEDS-YOU** | Requires Vercel API auth |
| 6 | Convex deployment identifier | ✅ DONE | Slug `adamant-stork-623` (`.convex.cloud` + `.convex.site`) |
| 7 | Schema hash | ✅ DONE | `convex/schema.ts` sha256 prefix `7935e45591570a61` |
| 8 | Vector-index configuration hash | ✅ DONE | `convex/rag/instance.ts` sha256 prefix `b419f71ed5a41871` (RAG component: dim 768, filters `[category, source]`) |
| 9 | Prompt hashes | ✅ DONE | `src/lib/prompt.ts` `9164c5a7b24da56b` (live); `convex/rag/prompts.ts` `8c2179786d4b2e3e` (dead code) |
| 10 | Configuration hashes | ✅ DONE | `crawl_config.json` `6ca073a84b681b9d`, `package.json` `5640ebc3e31e2977`, `http.ts` `629dcffc8ef88d66`, + 12 more in manifest |
| 11 | Package dependency snapshot | ✅ DONE | `pnpm-lock.yaml` sha256 prefix `63a2d2fef8389814` (392637 bytes); Node v24.12.0, pnpm 11.1.3 |
| 12 | Python dependency snapshot | ✅ DONE | Python 3.13.11; `pip freeze` captured (convex 0.7.0, Crawl4AI 0.8.6, curl_cffi 0.15.0, google-ai-generativelanguage 0.6.15, + others) |
| 13 | Document count | ⚠️ **NEEDS-YOU / NEEDS-CLI** | Requires admin Convex query (`api.admin.stats.getOverviewData` or `getDocumentCountByStatus`); local artifacts show only crawler-run stats (50 saved in last run), not current store row counts |
| 14 | Chunk count | ⚠️ **NEEDS-YOU / NEEDS-CLI** | Same as above |
| 15 | Embedding-model distribution | ⚠️ **NEEDS-YOU / NEEDS-CLI** | Would reveal how many chunks embedded with `gemini-embedding-2` — relevant to Track B freshness; queryable via `crawledChunks.embeddingModel` field |
| 16 | Corpus checksum | ✅ PARTIAL | `crawler_state.json` sha256 `b3bba4be...`; `dlq.jsonl` `32d371e3...`; `golden_set.jsonl` `33265a7d...`; `.agent/eval_baseline_pre_fix.json` `2960cbaa...` |
| 17 | Crawler-state checksum | ✅ DONE | see above |
| 18 | Evaluation-dataset checksum | ✅ DONE | `scripts/eval/golden_set.jsonl` `33265a7d...` (50 rows, degenerate — all `expected_url` = `"uettaxila.edu.pk"`) |
| 19 | Environment-variable names only | ✅ DONE | 21 keys listed in manifest (ADMIN_*, CEREBRAS_*, CLERK_*, CONVEX_*, CRAWL_*, CRON_*, GEMINI_*×3, GROQ_*, NEXT_PUBLIC_*, UPSTASH_*); **NO values recorded anywhere** |
| 20 | Provider account tiers | ⚠️ **NEEDS-YOU** | Groq tier (free/developer vs committed-spend enterprise) determines Aug 16 shutdown applicability — cannot be read from env |
| 21 | Backup or export reference | ⚠️ **NEEDS-YOU** | Existing backup ref / last backup time / availability not visible from local artifacts; **no new backup created** (bandwidth constraint + read-only Phase 0A) |
| 22 | List of every unavailable field | ✅ DONE | See "NEEDS-YOU" rows above + manifest `unavailable_or_unverifiable_fields_summary` |
| 23 | Confirmation no behavioral change | ✅ DONE | Zero code edits, zero deployments, zero re-embeds, zero crawl expansion, zero production writes, zero `main` commits. Only file moves (quarantine) + new doc writes under `docs/audit/`. |

---

## Critical findings requiring your decision before Phase 0B

### 1. Pre-existing fabricated metrics (CRITICAL INTEGRITY)
**19 untracked files** contained fabricated audit data generated by `scripts/build_audit_data.py` (hardcodes invented byte sizes + writes fake metrics like `recall_at_5: 0.857`). Per your decision: **quarantined to `audit/_contaminated_untracked_20260726-143830/`**, not committed, both generator scripts (`build_audit_data.py` + `eval/build_eval_dataset.py`) treated as suspect.
**Your action:** investigate provenance (who created these and when) before any quarantined file is trusted or restored.

### 2. Broken Convex CLI / toolchain (BLOCKER for Tracks B + C budget governance)
- `pnpm typecheck` fails: `tsc not recognized`
- `convex` CLI binary broken: `node_modules/convex/bin/main.js` MISSING (pnpm junction corruption — same root cause as prior session)
- **Impact:** blocks (a) baseline typecheck/vitest capture, (b) `npx convex deployment usage` bandwidth measurement that GOVERNS Track B/C budget, (c) in-process Convex queries for Track B freshness investigation
- **Remediation:** requires `node_modules` wipe + `CI=true pnpm install --frozen-lockfile` (~60s, destructive to working state). **NEEDS-YOUR-APPROVAL** — not executed in Phase 0A (read-only).

### 3. Gemini ingestion incident (Track B — cannot complete from local artifacts)
The freshness question "has UETGPT successfully contextualized/embedded anything since `gemini-2.0-flash` died on 2026-06-01?" **cannot be answered locally** — `crawler.log` shows no Gemini/embed/contextualize entries (the crawler only pushes markdown to `/ingest`; downstream Gemini calls happen in Convex and are invisible to the crawler log). Last crawler activity: 2026-07-14. Requires Convex queries (chunk `contextualizedText` population rate, `embeddingModel` distribution, error logs) — blocked on item #2 (CLI) or your admin auth.

### 4. Model migration scope is larger than initially scoped
Snapshot found `gemini-2.0-flash` at **5 sites** (not 3) — it's in the answer-gen fallback chain (`llm-models.ts:25`, `chat/models.ts:33`) in addition to contextualization. `llama-3.1-8b-instant` is at **13 sites** affecting more backend paths (intent, rewrite, HyDE, CRAG, faithfulness, rerank, eval-judge, multiVector) than the answer-gen model. Track C is bigger than the prior estimate.

---

## What Phase 0A did NOT do (per authorization)

- ❌ No behavioral/model/prompt/crawler/schema/index/deployment changes
- ❌ No new full backup (bandwidth constraint)
- ❌ No corpus-wide export
- ❌ No re-embedding
- ❌ No crawl expansion
- ❌ No `main` branch commit
- ❌ No `git push`, no `git add -A`, no force
- ❌ No contaminated files committed (all 19 quarantined, untracked)
- ❌ No env values recorded anywhere

---

## Recommended next actions (for your review — NOT started)

Per the approved three-track plan, the parallel work after Phase 0A approval is:

1. **Repair toolchain** (NEEDS-YOUR-APPROVAL): `node_modules` wipe + reinstall → unblocks bandwidth measurement + Track B Convex queries + baseline test capture.
2. **Track B — Gemini incident**: once CLI works, run targeted Convex queries (≤40MB budget) for contextualization/embedding freshness since Jun 1; then replace `gemini-2.0-flash` at 5 sites; one-document E2E smoke test (≤30MB).
3. **Track C — Groq substitutions**: replace both Llama models at 23 combined sites on an `agent/` branch; verify streaming vs structured-output split; task-specific smoke tests (negligible DB cost).
4. **Phase 0B-G gating**: merge the 8-item final addendum (multilingual reranking, bitemporal versioning, expanded ablation, leakage controls, parser scoring, Unicode/attribution defenses, cache lineage, resilience/SLO tests) into the written plan before governance work begins.

**Production rollout remains NO-GO** until Phase 7 canary + rollback gates pass AND a second reviewer exists for the high-impact held-out set.
