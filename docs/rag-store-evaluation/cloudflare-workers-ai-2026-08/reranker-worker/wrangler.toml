name = "uetgpt-reranker"
main = "worker.js"
compatibility_date = "2026-08-01"

# Workers AI binding. Inference is billed against the same 10,000 neurons/day
# free allocation as the corpus embedding, so corpus (re)embedding and
# query-time reranking share one budget - see report.md §6a for the depth/
# queries-per-day table.
[ai]
binding = "AI"
