# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
pnpm-debug.log*

# env files
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local crawl corpus output (raw cache + extracted documents; never committed,
# see docs/rag-store-evaluation/pdf-path-unification-2026-08/ and scripts/uet_crawler/corpus_sink.py)
/local_corpus_pilot/

# convex (generated files committed for Vercel builds)

# worktrees
/.worktrees

# graphify generated output
/graphify-out

# dist
/dist

.vercel

# logs (runtime artifacts must never be tracked)
*.log

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# local databases / caches (incl. accidentally-committed pnpm store indexes)
*.db
*.sqlite
*.sqlite*
**/index.db
.pnpm-store/

# agent / scratch / recon artifacts
/.agent/
/scratch/
/recon-report.md
eval_*.json

# test auth state — NEVER commit Clerk session tokens / cookies
**/.clerk/
/playwright/.clerk/

# debug captures
*-debug.png
