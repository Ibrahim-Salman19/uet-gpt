"""UET extraction corpus tooling.

- ``starter_set`` : the curated (family, url) registry, grounded in the crawl ledger.
- ``capture``     : captures real raw responses into immutable fixtures.
- ``bootstrap_expectations`` : generates a skeleton ``expected.json`` per fixture.
"""
