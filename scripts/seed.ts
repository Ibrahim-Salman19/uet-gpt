/**
 * Seed script — placeholder for Phase 2+.
 * Populates initial data (admin user, seed URLs, etc.).
 */

export async function main() {
  console.log("Seed placeholder — TODO: implement in Phase 2");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
