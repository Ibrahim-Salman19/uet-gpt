/**
 * URL validation script — placeholder for Phase 2+.
 * Validates seed URLs before crawling.
 */

export async function main() {
  console.log("URL validation placeholder — TODO: implement in Phase 2");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
